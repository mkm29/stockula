---
# Grafana dashboard provisioning for TimescaleDB monitoring
apiVersion: 1

providers:
  - name: 'TimescaleDB Dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
